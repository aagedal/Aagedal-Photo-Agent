# Caption Workspace speed-tools validation — 2026-08-21

Caption Workspace now uses the production edited/color-managed preview cache with current Camera
Raw settings and a bounded 3,840-pixel decode. It provides Fit and one-display-pixel-per-preview-
pixel modes, the existing app Full Screen entry, and overlays only confirmed named faces whose
persisted geometry is finite, positive, normalized, and associated with the exact image. Preview
controls restore the AppKit first responder rather than intentionally stealing caption focus.

The selected Deadline snapshot's caption configuration drives a compact ordered priority navigator
and Secondary & Technical disclosure. Each priority row focuses the durable MetadataPanel control,
shows the first inline validation issue, and exposes live Headline/Description counts plus the
narrowest optional maximum-length rule. Fix Next focuses the next blocker, then the first remaining
issue. An unresolved referenced validation profile safely falls back to current requirements rather
than pretending the reference was resolved.

The adaptive sticky action bar includes Previous, Save & Next, Write & Next, template Replace/
Append, Copy Previous Replace/Append, Fix Next, More, and Close. Write & Next crosses the durable
flush/write boundary and advances only after success for the unchanged URL. Separate persisted
Caption shortcuts default to Control-Return and Control-Option-Return, reject conflicts, and route
through an IME-aware local monitor that ignores marked composition and repeats. System spelling and
grammar are enabled only for free-text caption fields and reset off for names, controlled values,
identifiers, and codes.

Confirmed-person ordering is derived only from persisted named identity groups and usable geometry;
uncertain/suggestion-only evidence is omitted rather than inferred. Nine speed-tool tests, thirteen
Caption session tests, and nine accessibility/keyboard tests passed together: 31 logical tests with
no failures. The production target compiled under Swift 6, and parse/diff checks passed.

The compact surface navigates to established stateful MetadataPanel controls rather than physically
rebuilding/reordering them. “100%” is bounded-preview resolution rather than an unbounded native
decode. Referenced validation-profile resolution, a tiled native-pixel loupe, and hands-on focus,
IME, VoiceOver, spelling, overlay-rotation/crop, and narrow-window observations remain explicit.
